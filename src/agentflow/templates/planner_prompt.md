# Built-in Planner Prompt

Inspect only the supplied context. Use the smallest context needed for a correct plan and avoid restating repository material that is already explicit in the input. Produce a structured plan that maps implementation steps to acceptance criteria and identifies validation steps, risks, and affected paths.
